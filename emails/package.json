{
  "name": "otodo-emails",
  "version": "1.0.0",
  "scripts": {
    "build": "mjml ./src/* -o ../server/src/main/resources/emails --config.minify true",
    "start": "npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "mjml": "5.3.0"
  }
}